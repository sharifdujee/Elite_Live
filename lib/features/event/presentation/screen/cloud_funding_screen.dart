import 'package:elites_live/core/global/custom_text_view.dart';
import 'package:flutter/material.dart';

class CloudFundingScreen extends StatelessWidget {
  const CloudFundingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: CustomTextView("Cloud Funding screen"),
      
    );
  }
}
